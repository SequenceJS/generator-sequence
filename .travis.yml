language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-sequence' ]; then cd .. && eval "mv $currentfolder generator-sequence" && cd generator-sequence; fi

